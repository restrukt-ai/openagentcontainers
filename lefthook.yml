pre-commit:
  commands:
    tidy:
      run: task tidy
    lint:
      run: task lint
    test:
      run: task test
    spec-build:
      run: task spec:build

commit-msg:
  commands:
    commitlint:
      run: commitlint --edit {1}
