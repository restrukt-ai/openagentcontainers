pre-commit:
  commands:
    pkg-lint:
      run: task pkg:lint
    pkg-tidy-check:
      run: task pkg:tidy:check
    cli-lint:
      run: task cli:lint
    cli-tidy-check:
      run: task cli:tidy:check

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
