{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SupportTicket",
  "description": "An inbound support ticket event delivered to the support agent.",
  "type": "object",
  "required": ["ticket_id", "customer_id", "subject", "body", "created_at"],
  "additionalProperties": false,
  "properties": {
    "ticket_id": {
      "type": "string",
      "description": "Unique identifier for the ticket."
    },
    "customer_id": {
      "type": "string",
      "description": "Identifier of the customer who submitted the ticket."
    },
    "subject": {
      "type": "string",
      "description": "Short summary of the issue."
    },
    "body": {
      "type": "string",
      "description": "Full text of the customer's message."
    },
    "priority": {
      "type": "string",
      "enum": ["low", "normal", "high", "urgent"],
      "description": "Ticket priority assigned by the upstream system."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the ticket was created."
    }
  }
}
